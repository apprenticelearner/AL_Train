import React from "react";
import ReactDOM from "react-dom/client";
import ALTrain from "./altrain";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ALTrain/>
  </React.StrictMode>
);

